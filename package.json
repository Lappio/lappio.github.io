{
  "name": "lappio-blog",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/clean-site.mjs && eleventy",
    "dev": "node scripts/clean-site.mjs && eleventy --serve",
    "write": "node scripts/writer-server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6",
    "gray-matter": "4.0.3",
    "jsdom": "26.1.0",
    "markdown-it": "^14.3.2"
  }
}
